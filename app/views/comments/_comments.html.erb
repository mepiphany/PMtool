<div id="<%= dom_id(comment) %>">
<%= comment.body %>
<%= link_to "Edit", edit_discussion_comment_path(@discussion, comment)  %>
<%= button_to "Delete", discussion_comment_path(@discussion, comment), method: :delete, remote: true %>
<hr>
</div>
