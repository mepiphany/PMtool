<%= @discussion.errors.full_messages.join(", ") %>

<%= simple_form_for [@project, @discussion], remote: true do |f|  %>
    <%= f.input :title %>
    <%= f.input :body %>
    <%= f.submit class: "btn btn-primary" %>
<% end %>
